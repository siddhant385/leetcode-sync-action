{
  "name": "scripts",
  "version": "1.0.0",
  "description": "This repository contains the standalone GitHub Action script for syncing LeetCode submissions directly to a GitHub repository.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "type": "commonjs",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@vercel/ncc": "^0.44.1",
    "typescript": "^5.5.4"
  }
}
